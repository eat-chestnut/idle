# 《山海巡厄录》第一阶段测试与验收说明

## 1. 文档目的

本文档用于把第一阶段当前真实可执行的测试矩阵、统一验收入口、文档分工和最小自测顺序收口到一处。

本文档只记录当前仓库已经稳定落地的能力，不扩写未来系统。

---

## 2. 当前统一口径

第一阶段验收统一以当前真实 backend 为准：

- 路由以 `backend/routes/api.php`、`backend/routes/web.php`、`backend/routes/console.php` 为准
- 请求与响应以各 `ApiRequest`、`Resource`、`Admin` 控制器为准
- 错误码以 `backend/app/Support/ErrorCode.php` 与《错误码总表》为准
- 枚举值以 `backend/app/Enums` 与《枚举总表》为准
- 机器可读契约以 `backend/docs/api/phase-one-frontend.openapi.json` 为准
- 人读示例与联调说明以根目录 `doc/codex/*.md` 为准

如果代码、测试、OpenAPI 和根目录文档出现漂移，处理顺序必须是：

1. 先核对真实代码
2. 再修正测试 / OpenAPI 契约
3. 最后回写根目录正式文档

---

## 3. 最小测试矩阵

| 验收点 | 当前真实覆盖 | 说明 |
| --- | --- | --- |
| 前台 API Bearer Token 认证 | `backend/tests/Feature/Api/PhaseOneAuthenticationApiTest.php` | 覆盖当前 11 个 phase-one 前台路由全部未登录拦截口径 |
| 前台接口身份来源只能取认证上下文 | `backend/tests/Feature/Api/PhaseOneAuthenticationApiTest.php` | 覆盖角色创建忽略前端 `user_id`、角色与装备归属边界 |
| 角色创建与 12 固定槽位初始化 | `backend/tests/Feature/Api/PhaseOneCharacterEquipmentApiTest.php` | 覆盖创建角色、角色详情、固定 12 槽位初始化 |
| 换装 / 卸装主副武器联动 | `backend/tests/Feature/Api/PhaseOneCharacterEquipmentApiTest.php` | 覆盖双手武器清理副手、卸下主手联动清理副手 |
| 战斗准备正式结果 | `backend/tests/Feature/Api/PhaseOneBattlePrepareApiTest.php` | 覆盖章节、难度、首通状态摘要、prepare 返回 battle context / 属性 / 怪物 / 槽位快照 |
| 战斗结算正式闭环 | `backend/tests/Feature/Api/PhaseOneBattleSettlementApiTest.php` | 覆盖掉落、首通奖励、重复结算保护、上下文归属校验、失败错误码 |
| 首通奖励状态变化 | `backend/tests/Feature/Api/PhaseOneBattlePrepareApiTest.php` `backend/tests/Feature/Api/PhaseOneBattleSettlementApiTest.php` `backend/tests/Feature/Api/PhaseOnePlayerJourneySmokeTest.php` | 覆盖未领取、已成功、失败沉淀三种状态 |
| failed reward_grant 沉淀与 retry 承接 | `backend/tests/Feature/Api/PhaseOneBattleSettlementApiTest.php` `backend/tests/Feature/Admin/PhaseOneAdminPagesTest.php` | 覆盖 failed 落库、battle context 不可重放、后台按 `reward_grant_id` 与业务来源补发 |
| workflow lock 关键语义 | `backend/tests/Unit/Support/WorkflowLockServiceTest.php` `backend/tests/Feature/Console/WorkflowLockCheckCommandTest.php` `backend/tests/Feature/Api/PhaseOneBattleSettlementApiTest.php` `backend/tests/Feature/Admin/PhaseOneAdminPagesTest.php` | 覆盖 lock 自检、busy 拦截、lock 能力缺失错误码 |
| 后台环境诊断 / 运维关键路径 | `backend/tests/Feature/Console/PhaseOneEnvironmentDiagnoseCommandTest.php` `backend/tests/Feature/Admin/PhaseOneAdminPagesTest.php` | 覆盖环境诊断命令、后台查询页、补发、修复、配置页访问 |
| 玩家主链闭环烟测 | `backend/tests/Feature/Api/PhaseOnePlayerJourneySmokeTest.php` | 覆盖创建角色、换装、prepare、settle、首通奖励状态变更 |
| OpenAPI 契约与真实路由同步 | `backend/tests/Feature/Api/PhaseOneFrontendContractArtifactTest.php` | 覆盖公开路由和关键 request 字段与契约文件同步 |

---

## 4. 统一验收入口

推荐从 `backend/` 目录执行以下单命令：

```bash
composer phase-one:acceptance
```

该入口会顺序执行：

1. `php artisan phase-one:diagnose`
2. 第一阶段定向验收测试集

当前聚合脚本位置：

- `backend/scripts/phase-one-acceptance.sh`

---

## 5. 最小执行前置

执行统一验收前，建议先完成：

```bash
cd backend
composer install
php artisan migrate:fresh --seed
```

当前最小联调测试用户：

- `users.id = 2001`
- `Authorization: Bearer test-token-2001`

---

## 6. 建议验收顺序

若需要人工分步验收，建议按以下顺序执行：

1. `php artisan phase-one:diagnose --json`
2. `php artisan workflow-lock:check --json`
3. `php artisan test tests/Feature/Api/PhaseOnePlayerJourneySmokeTest.php`
4. `php artisan test tests/Feature/Api/PhaseOneBattleSettlementApiTest.php`
5. `php artisan test tests/Feature/Admin/PhaseOneAdminPagesTest.php`
6. 最后执行 `composer phase-one:acceptance`

---

## 7. 文档分工

为了避免双份文档继续漂移，后续统一按以下分工维护：

- `backend/docs/api/phase-one-frontend.openapi.json`
  机器可读正式契约产物，用于 route / request / response / 测试同步校验
- `backend/docs/api/README.md`
  backend 侧联调入口、契约使用说明、验收命令入口
- `doc/codex/接口示例文档.md`
  根目录人读版正式示例文档，只保留当前真实存在的 phase-one 前台接口
- `doc/codex/认证与接口公共规则.md`
  认证、响应外壳、分页、时间等公共约束
- `doc/codex/第一阶段测试与验收说明.md`
  测试矩阵、验收入口、最小自测顺序

维护规则：

1. 先改 backend 真实实现
2. 再改测试与 OpenAPI
3. 最后回写根目录正式文档

---

## 8. 当前结论

当前第一阶段已经形成如下统一收口链路：

- 代码实现
- 定向测试
- OpenAPI 契约
- 根目录正式文档
- 本地统一验收入口

后续继续迭代时，应优先维持这五者同步，而不是单独补某一份文档或只加零散 case。
